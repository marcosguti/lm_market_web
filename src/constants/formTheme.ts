/** Shared form control dimensions (parity with mobile AppFormTheme). */
export const OTP_CELL_SIZE_PX = 36;

/** Number of digits for email / login verification codes. */
export const OTP_DIGIT_COUNT = 4;
